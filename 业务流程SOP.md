# 阳光校园健康 · 儿童体检全流程业务 SOP

> 版本：v1.0 ｜ 适用对象：产品、运营、实施、以及需要复现/扩展本系统的 AI 工具
> 说明：本文档以“业务流转 + 页面交互 + 数据规则”三层描述，可直接作为其他 AI 工具的生成与调用输入。
> 技术形态：TanStack Start + React 19 + Tailwind v4，移动端手机壳预览（`MobileFrame`），全部数据为原型 Mock。

---

## 0. 系统总览

### 0.1 角色与入口

| 角色 | 入口路由 | 使用者 | 核心职责 |
|---|---|---|---|
| 家长端 | `/parent` | 学生家长 | 授权签署、报告查看、AI 解读、方案执行、打卡、预约挂号 |
| 学校端 | `/school` | 校医（保健老师）/ 班主任 / 体育老师 | 排程、授权催办、缺检补检、异常跟进、任务闭环、体适能录入 |
| 医生端 | `/doctor` | 体检医生 / 健康管理师 | 现场录检、质控、危机值处置、复核、方案下发 |
| 社区端 | `/community` | 社区卫生服务中心 | 患儿上传、在管随访、健康咨询、宣教 |
| 管理端 | `/admin`、`/bigscreen` | 区教育局 / 卫健委 | 区域数据看板、大屏 |

角色选择页：`/`（`src/routes/index.tsx`），四个角色卡片直接跳转各端。

### 0.2 端内导航（底部 Tab）

- 家长端：首页 `/parent` · 健康方案 `/parent/health-plan` · 健康助手 `/parent/comm` · 我的 `/parent/me`
- 学校端：工作台 `/school` ·（校医专属）今日体检 `/school/today` · 学生/我的班级 `/school/students` · 任务 `/school/intasks` · 我的 `/school/me`
- 医生端：工作台 `/doctor` · 用户 `/doctor/exam` · 方案 `/doctor/plan` · 沟通 `/doctor/comm` · 我的 `/doctor/me`
- 社区端：工作台 `/community` · 在管患者 `/community/patients` · 健康咨询 `/community/consult` · 我的 `/community/me`

### 0.3 主业务链路（端到端）

```text
教育局下发体检计划
   → 学校端排程 + 家长授权签署（教育局制式协议）
   → 现场体检（设备自动采集 + 医生手动录入 + 质控）
   → 异常/危机值现场处置闭环
   → 医生复核 → 生成体检报告 + 五色风险分级
   → 家长端报告推送 → AI 解读 → 异常项一键咨询医生（置顶挂号卡）
   → 生成健康方案（医生结构化编辑 → 下发）
   → 家庭执行：饮食/运动打卡 + 呵护任务提醒
   → 学校端跟进异常/复查任务
   → 高风险患儿上传社区 → 社区随访/一键推送儿童医院专科
   → 复查回流，风险等级重评
```

---

## 1. 统一业务规则（跨端一致）

### 1.1 五色风险分层梯度（`src/lib/risk-levels.ts`）

所有端使用同一套 `RiskKey`，徽章均可点击弹出 `RiskLevelSheet` 查看全部等级注释。

| 等级 | 色值 | 风险等级 | 下一步动作 | 责任主体 | 判定依据 |
|---|---|---|---|---|---|
| 绿色 | #22a35b | 正常 | 健康教育 + 年度复评 | 学校 + 家庭 | 全部指标在同龄同性别参考区间内 |
| 蓝色 | #2f6fe0 | 轻度风险 | AI 提醒 + 家庭自管 | 家庭 + AI | 1 项指标临界，无临床症状 |
| 黄色 | #f0b429 | 重点关注 | 学校/家庭/健管师干预 | 学校 + 健管师 | 1–2 项轻中度异常，需行为干预 |
| 橙色 | #f2820c | 医学复核 | 线上/线下复核 | 儿童医院专科 | 明显异常或复测仍异常 |
| 红色 | #e03131 | 高风险/危机 | 绿色通道 + 危机处置 | 医院 + 健管师 + 校家 | 触发危急值阈值或急性风险 |

交互约定：
- 家长端报告页顶部风险条 + 3D 形象上的浮动徽章，点击均打开等级说明弹层，高亮当前等级并展示“面向家长的通俗注释”。
- 社区端风险列表徽章同样可点击，展示同一套标准。

### 1.2 体检项目与设备映射（`src/lib/exam-record.ts`）

| 序号 | 项目 | 采集方式 | 设备 | 参考范围（9 岁示例） |
|---|---|---|---|---|
| 1 | 身高 | auto | 身高体脂分析仪 | 125–142 cm |
| 2 | 体重 | auto | 身高体脂分析仪 | 24–34 kg |
| 3 | 体脂率 | auto | 身高体脂分析仪 | 12–24 % |
| 4 | 视力（左） | auto | 视力筛查仪 | 5.0–5.3 |
| 5 | 视力（右） | auto | 视力筛查仪 | 5.0–5.3 |
| 6 | 腰围 | auto | 腰臀围尺 | 50–63 cm（腰高比 < 0.48） |
| 7 | 臀围 | auto | 腰臀围尺 | 60–76 cm |
| 8 | 血压 | auto | 血压测量仪 | 收缩 85–115 / 舒张 55–75 mmHg |
| 9 | 脊柱侧弯 ATR | manual | 脊柱侧弯测量尺 | 0–5° |
| 10 | 口腔·龋齿 | manual | 医生目测 | 正常 / 龋齿 1-2 颗 / 龋齿 3+ 颗 / 牙龈异常 |

状态判定 `evalItem`：`normal` / `abnormal` / `empty`；超范围自动置 `retest = true`（标记需重测）。

### 1.3 危机值判定与处置（仅 3 类高危指标）

只有严重影响健康的指标进入强制闭环，其余仅做异常提示。

| 指标 | 分级 | 触发条件 | 处置时限 | 必须由体检医生确认的 3 步 |
|---|---|---|---|---|
| 血压 | 危急值 | 收缩压 ≥ 130 mmHg | 立即（30 分钟内） | ①静坐 5 分钟换袖带复测确认均值 ②完成头痛/头晕/视物模糊症状评估 ③确认已通知家长并开具心内科转诊 |
| 视力（左/右） | 预警值 | 裸眼视力 ≤ 4.8 | 本次录检内 | ①单眼遮盖复测一次确认读数 ②确认已询问配镜与用眼情况 ③确认已开具眼科转诊建议 |
| 脊柱侧弯 | 预警值 | ATR ≥ 7° | 24 小时内 | ①前屈试验复测 ATR 确认角度 ②确认已检查双肩/骨盆对称性与身高变化 ③确认已开具骨科（脊柱专科）转诊并告知家长 |

闭环规则（硬约束）：
1. 命中规则时，该项卡片顶部出现红色/橙色告警条（🚨 危急值 / 🛡️ 预警值）。
2. 数字键盘被“处置方案面板”替换，医生必须逐条勾选 3 个步骤。
3. 未勾满时底部按钮为「危机待处置」且置灰，不能进入下一项。
4. 复核页底部汇总“已检出 N 项 / 未闭环 M 项”，只要 M>0 即禁止提交。
5. 每次闭环写入不可篡改的质控留痕（操作人 + 时间戳）。

---

## 2. 家长端 SOP（`/parent`）

### 2.1 授权与绑定
1. 首次进入 `/parent` 弹出 **《学生健康体检授权协议书》（南京市教育局制式）**，为第一份必签协议。
   - 结构化条款：数据范围（基本信息 / 体检数据 / 日常记录）、使用目的、共享方（学校、体检机构、社区）、撤回方式。
   - 未同意不展示体检数据。
2. 孩子绑定：`/parent/bind`（学号 + 姓名校验）；支持多孩切换（首页顶部头像 Chip：小阳 / 小雨）。
3. 数据权限管理：`/parent/mydata` 查看已授权范围；`/parent/terminate` 撤回授权/解绑。

### 2.2 报告查看与解读（`/parent/report`）
1. 头部：孩子姓名、体检日期、学校班级。
2. **风险总览卡**：五色等级 + 等级注释 + 下一步 + 责任主体，点击展开完整等级说明。
3. 3D 虚拟儿童形象 + 异常部位标注。
4. 分组指标：体格发育 / 视力与眼健康 / 口腔 / 内科 / 过敏与呼吸。每项含数值、参考区间、色点（绿/黄/红）、参考来源。
5. 异常项附带 **推荐科室 + 推荐医生**，并提供「立即咨询」。
6. 下一步三选一：AI 解读报告 / 就异常项咨询医生 / 生成专属健康方案。
7. 历史档案时间轴：按年份展示 体检 / 复查 / 就诊 三类记录。

### 2.3 异常项「立即咨询」→ AI 会话（`/parent/comm`）
- 跳转参数：`?topic=<异常项>&dept=<科室>&doctor=<医生>&from=report`。
- 交互规则：
  1. 命中医生时，会话顶部 **固定置顶** 该医生的互联网预约挂号卡片（不随消息滚动）。
  2. **不自动追加任何提问**，输入框保持空白，由家长自行发问。
  3. 隐藏默认推荐问题面板，避免干扰。
- 无医生参数时（如快捷入口饮食/运动/睡眠咨询）：携带 `?q=` 自动发问并立即作答。

### 2.4 健康方案执行（`/parent/health-plan`）
1. 自选干预周期：3 / 5 / 7 周。
2. 方案结构与医生端一致：建议概述 / 饮食 / 运动 / 居家呵护 / 睡眠 / 复查。
3. 生成后进入执行期，产出每日任务。

### 2.5 每日打卡（首页任务行）
- 仅两类：**饮食打卡**、**运动打卡**（`src/components/CheckinSheets.tsx`）。
- 交互：点击在当前页底部弹窗填写，**不跳转、不支持事后补卡**。
- 饮食：餐次 + 拍照/语音/文字三种记录方式 + 估算 kcal。
- 运动：项目 + 时长 + 强度（轻松/适中/吃力）+ 疲惫度 1–5。
- 完成后任务行显示摘要（如「晚餐 · 拍照记录 · 约 560 kcal」）。

### 2.6 呵护任务（`/parent/care`）
按周期提醒的家庭事项：晨起体重记录(7d)、床品除螨清洗(14d)、开窗通风(1d, 被动)、加湿器换水(3d)、牙刷更换(90d)、维生素 D(1d, 被动)。逾期高亮，被动项只提醒不需操作。专题页示例：`/parent/dustmite`。

### 2.7 复查与通知
- `/parent/review`：复查计划、预约、结果回传。
- `/parent/notice`：学校/医院通知。
- `/parent/record`：历次记录归档。

---

## 3. 学校端 SOP（`/school`）

### 3.1 视角切换（`src/lib/school-role.ts`）
三种视角，`sessionStorage` 持久化：

| 视角 | 可见范围 |
|---|---|
| 保健老师（校医） | 全校；含「今日体检」；学生页含年级/班级筛选；任务页含 AI 派单与角色 Tab |
| 班主任 | **隐藏「今日体检」**；学生页仅本班（示例 3年3班）且隐藏年级/班级筛选，Tab 名为「我的班级」；任务页仅显示指派给本人或本班的任务，隐藏 AI 派单卡与角色 Tab |
| 体育老师（`pe`） | 工作台以体测为主，入口「体适能录入」（`/school/fitness`）；按班级录入成绩、查看薄弱学生名单 |

### 3.1.1 体适能录入（`/school/fitness`，`src/lib/fitness.ts`）
1. 六项体测：50 米跑、坐位体前屈、立定跳远、1 分钟跳绳、1 分钟仰卧起坐、肺活量。
2. 按班级选择学生 → 逐项录入成绩 → 系统按**性别 + 项目**评分规则自动评级（优秀/良好/及格/不及格）。
3. 汇总班级达标率与薄弱项，成绩同步进入学生健康档案，与体检数据共同构成孩子档案。

### 3.2 体检前
1. `/school/today`：当日排程、分组、检台状态、进度（校医专属）。
2. 授权催办：未签署名单，一键推送提醒到家长端。
3. `/school/absent`：缺检学生登记与补检安排。

### 3.3 体检中
- 现场协同：叫号、分流、异常学生现场标记。

### 3.4 体检后
1. `/school/abnormal`：异常学生清单，按项目/等级筛选。
2. `/school/focus`：重点关注名单（黄/橙/红）。
3. `/school/escalated`：已上报/转诊学生。
4. `/school/observe`：观察期学生跟踪。
5. `/school/notify`：向家长批量下发通知。
6. `/school/intasks`：任务闭环（催授权、催复查、家校沟通），状态：待办 → 进行中 → 已完成。
7. `/school/report`：班级/年级体检汇总报表。
8. `/school/student/$id`：单个学生档案（体检史 + 风险 + 任务）。

---

## 4. 医生端 SOP（`/doctor`）

### 4.1 检前准备
- `/doctor/prep`：设备校准（标准砝码/标准杆/视力表照度）、耗材、人员分工。
- `/doctor/exam`（工作台入口名称：**体检录入**）：当前体检医生 + 本工位负责的体检项 + 扫码入口 + 学生队列。

#### 4.1.1 设备账号（工位）模型（`src/lib/exam-stations.ts`）

每台体检设备绑定一个账号，医生用该账号登录后**只看到本工位负责的体检项**。

| 工位 | 账号 | 绑定设备 | 负责体检项 | 医生 |
|---|---|---|---|---|
| 一号工位 · 形体与视力 | DEV-BODY-01 | 身高体脂分析仪、视力筛查仪 | height / weight / bodyfat / vision-l / vision-r | 陈医生 |
| 二号工位 · 围度与血压 | DEV-CIRC-02 | 腰臀围尺、血压测量仪 | waist / hip / bp | 林医生 |
| 三号工位 · 脊柱与口腔 | DEV-MANU-03 | 脊柱侧弯测量尺（手工）、口腔目测 | spine / oral | 赵医生 |

默认登录工位 `CURRENT_STATION_ID = "s1"`；`findStation()` 按 id/账号查找，`stationItems()` 过滤本工位体检项。录检页通过 `?station=<id>` 传递工位。

#### 4.1.2 体检录入页交互（`/doctor/exam`）
1. 顶部只突出：**当前体检医生** + 其负责的体检项标签（不展示账号切换）。
2. 流程提示条：`扫码 → 学生上机采集 → 医生核对设备读数 → 逐项确认 → 提交并同步`。
3. 扫码入口（二维码/条码）：点击后进入模拟扫码遮罩，识别学生后携带 `station` 跳转 `/record/$id`。
4. 队列筛选：**全部 / 待检 / 进行中 / 已检**，各分组显示人数；标题同时显示待检与已检人数。
   - 「已检」包含状态：`已检-正常`、`已检-异常`、`需复核`、`方案确认`。
5. 点击行为按状态区分：
   - 待检 / 进行中 → `/record/$id?station=<工位>` 继续录检；
   - 已检 → `/doctor/child?sid=<学号>` 查看该学生的体检详情档案（姓名、学号、班级、性别年龄、状态、风险标签、各项明细与趋势，并提供「返回体检录入」）。


### 4.2 现场录入（沉浸式刷屏版 `/record/$id`）
1. 每项一屏，**逐项滚动吸附**，禁止一次滑过多项。
2. 顶部：学生姓名、学号、队列位置、进度（已录 X/10、异常 N、需重测 M）。
3. **按工位过滤**：只显示 `?station=` 对应工位负责的体检项（如一号工位仅 5 项）。
4. 设备自动项：进入时已预填读数；超范围自动标记「需重测」。
5. **设备读数核对（自动采集项必做）**：卡片提供「请核对设备读数与本机数据是否一致」按钮，医生点击后变为「已核对」；未核对时点击「确认 · 下一项」会被拦截并提示先核对。手工项不受此约束。
6. 手动项（脊柱 ATR、龋齿）：数字键盘 / 选项卡录入；提供「一键按正常填充」批量处理未填手动项。
7. 底部按钮：`标重测` + `确认 · 下一项`（已移除 ⚡ 恢复设备读数按钮）。
8. 命中危机值 → 键盘替换为处置方案面板，勾满 3 步方可继续（见 1.3）。
9. 提交（按钮：**提交并同步**）：
   - 有未闭环危机值 → 阻止提交并跳回该项。
   - 有危机值已闭环 → 提示「N 项危机值已按处置方案闭环并上报台账」。
   - 有需重测 → 提示「已回传班主任，通知家长带学生返场重测」。
   - 全部正常 → 「已提交并同步」至健康档案。
   - 提交后返回 `/doctor/exam?view=queue`，医生**重新扫码下一位**（不再自动跳到下一位）。

### 4.3 分步质控录入版（`/doctor/entry/$id`）
1. 固定步骤条，不可横滑跳项，未质控通过不能进入下一项。
2. 每个节点头部：设备校准信息、室内质控卡片、本项危机值判定标准（分级/阈值/时限/闭环步数）示例卡。
3. 阈值超限 → 质控提示 → 触发双人复测（记录复测值 + 双人核对体征）。
4. 手动文本项支持语音输入（`VoiceTextarea`，🎤）。
5. 底部质控小结：本项结论 + 危机值状态（如「危机：🚨 2 项待处置」）。
6. 质控留痕区：校准、复核、阈值触发、危机闭环全部带时间戳且不可篡改。

### 4.4 复核与风险评定
- `/doctor/review`：逐生复核录入结果，汇总危机值检出/未闭环数量，未闭环禁止提交。
- `/doctor/qc`：质控看板（复测率、异常率、留痕完整率）。
- `/doctor/riskreview`：风险等级评定与调级（输出五色等级）。
- `/doctor/focus`：重点儿童名单。

### 4.5 方案确认与下发（`/doctor/plan`）
1. 结构化板块（各自独立卡片 + 图标）：
   - 🧭 建议概述 · 🍚 饮食 · 🏃 运动 · 🏠 居家呵护 · 🌙 睡眠 · 📅 复查随访
2. 每个板块支持内联编辑，编辑控件为 `VoiceTextarea`（语音或文字均可修改）。
3. 头部提供「查看小阳的详情档案」快捷跳转 `/doctor/child`。
4. 审批/版本历史：记录每次修改版本与审核人。
5. 确认下发后同步至家长端 `/parent/health-plan`。

### 4.6 转诊与随访
- `/doctor/referral`：开具转诊（科室 + 医生 + 绿色通道）。
- `/doctor/followup`：随访计划与执行。
- `/doctor/coord`：校医-体检医生-健管师协同。
- `/doctor/comm` / `/doctor/messages`：与家长/学校的沟通与消息。

---

## 5. 社区端 SOP（`/community`）

### 5.1 工作台（`/community/index`）
统计卡片：在管患者、**患儿上传**（原「医院下传/风险患儿」，🚨 rose 色）、待随访、健康咨询。

### 5.2 患儿上传 / 高风险列表（`/community/risk`）
1. 列表展示高风险患儿（示例：BMI ≥ P95、哮喘控制不佳、FeNO 升高）。
2. 风险等级使用统一五色标准（红色 / 橙色），徽章可点击查看等级说明。
3. 科室筛选：全部 / 呼吸科 / 肥胖科。
4. 展开「风险问题明细」：指标实测值、参考值、临床备注。
5. **一键推送**：按问题类型推送至儿童医院对应专科（呼吸科 / 肥胖内分泌科），推送成功 Toast 反馈。

### 5.3 在管与随访
- `/community/patients`、`/community/patient/$id`：在管档案、服务包、随访记录。
- `/community/consult`：家长在线咨询。
- `/community/edu`：健康宣教内容分发。

---

## 6. 管理端 / 大屏

- `/admin`：区域管理（学校、计划、账号、指标配置）。
- `/bigscreen`：区域体检大屏（完成率、异常率、五色分布、危机值台账）。

---

## 7. 状态机汇总

### 7.1 学生体检状态
`待检 → 进行中 → 待复测（超范围） → 已完成 → 已出报告`
缺检分支：`缺检 → 补检安排 → 待检`

### 7.2 危机值状态
`检出 → 处置中（勾选 0/3 · 1/3 · 2/3）→ 已闭环（3/3）→ 上报台账`

### 7.3 任务状态（学校端 / 家长端）
`待办 → 进行中 → 已完成 / 已逾期`

### 7.4 健康方案状态
`AI 生成草案 → 医生结构化编辑 → 确认下发 → 家长执行中 → 周期结束复评`

---

## 8. 交互与 UI 约定

1. 所有页面在 `MobileFrame` 手机壳内渲染，屏高上限 `min(1311px, 100svh-140px)`（较原始 iPhone 尺寸放大 1.5×）。
2. 颜色一律使用语义 token：`success / warning / danger / teal / deep / warm / rose / surface`；禁止硬编码颜色类。
3. 底部弹层统一用 `ActionSheet`；风险说明用 `RiskLevelSheet`；打卡用 `CheckinSheets`。
4. 反馈统一用 `sonner` 的 `toast`，成功/错误均带 `description` 说明后续动作。
5. 图标以 `EIcon`（emoji）为主，配合 `lucide-react` 线性图标。
6. 语音输入统一用 `VoiceTextarea`（`webkitSpeechRecognition`，追加式写入）。
7. 切换学生/切换页面时强制复位滚动位置，避免路由滚动恢复串位。

---

## 9. 关键文件索引（供 AI 工具调用）

| 领域 | 文件 |
|---|---|
| 五色风险等级 | `src/lib/risk-levels.ts`、`src/components/RiskLevelSheet.tsx` |
| 体检项 / 危机值规则 | `src/lib/exam-record.ts` |
| 体检队列用户 | `src/lib/exam-users.ts` |
| 学校端视角状态 | `src/lib/school-role.ts` |
| 班级数据 | `src/lib/teacher-class.ts` |
| 打卡记录 | `src/lib/checkin.ts` |
| 社区患者 | `src/lib/community-patients.ts` |
| 通用 Mock | `src/lib/mock-data.ts` |
| 手机壳 / 状态栏 | `src/components/MobileFrame.tsx` |
| 底部导航 / 工作台卡片 | `src/components/BottomNav.tsx`、`src/components/Workbench.tsx` |
| 语音输入 | `src/components/VoiceTextarea.tsx` |
| 沉浸式录检 | `src/routes/record.$id.tsx` |
| 分步质控录检 | `src/routes/doctor.entry.$id.tsx` |
| 结构化方案 | `src/routes/doctor.plan.tsx` |
| 家长报告 | `src/routes/parent.report.tsx` |
| 家长 AI 会话 | `src/routes/parent.comm.tsx` |
| 社区高风险 | `src/routes/community.risk.tsx` |

---

## 10. 扩展约束（给后续 AI 生成器的硬性要求）

1. 新增风险相关 UI 必须复用 `RISK_LEVELS`，不得自造等级或色值。
2. 新增危机值规则必须写入 `CRIT_RULES`，且 `plan` 恰好 3 条、每条以「体检医生…」开头、均为现场可确认动作。
3. 危机值未闭环时任何提交入口必须被阻断。
4. 家长端异常项咨询跳转不得自动填充或自动发送提问。
5. 学校端任何新页面必须先判断 `school-role` 视角，班主任视角只见本班数据。
6. 新增体检项必须归属现有 5 类设备或标注为手工录入。
7. 所有新增页面须新建 `src/routes/*.tsx` 路由文件，并配置独立 `head()` 元信息。
